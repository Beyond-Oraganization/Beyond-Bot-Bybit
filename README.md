# BeyondBot_Bybit

A .NET console application for Windows.

## Prerequisites

- .NET 9.0 SDK

## Build

Run `dotnet build` to build the project.

## Run

Run `dotnet run` to execute the application.

## Debug

Use VS Code to debug by pressing F5 or using the debug panel.